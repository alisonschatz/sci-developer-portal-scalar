# Chaveado pelo nome EXATO da tag (tags[].name) no spec de origem desta API.
# Tag sem entrada aqui mantém a descrição original vinda do backend.
#
# Limpo de propósito (2026-07) — sem texto personalizado ainda pronto.
# Descrições de tag são conteúdo de "engenharia de escrita voltada ao
# cliente" (detalhe de permissão por recurso) — entram aqui quando
# prontas.
#
# ⚠️ Nota para quando isso for retomado: a tag "Feriado" tinha uma
# correção aqui (a descrição original do backend dizia "criação, edição
# e exclusão", mas esse recurso é só leitura via API). Removida nesta
# limpeza — a descrição original (potencialmente enganosa) volta a
# aparecer até esse texto ser reescrito. Ver docs/arquitetura.md,
# decisão 12.
#
# ExemploDeTag:
#   description: |
#     O que esse grupo de endpoints permite fazer, e por qual perfil.
