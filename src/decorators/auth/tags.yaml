# Chaveado pelo nome EXATO da tag (tags[].name) no spec de origem desta API.
# Nome confirmado no spec real de produção (auth.json): "Autenticação".

Autenticação:
  description: |
    Geração e renovação de token JWT a partir das credenciais de parceiro
    e cliente. Todo token gerado aqui fica disponível automaticamente para
    as demais APIs deste portal — não é preciso copiar e colar manualmente.
