# Chaveado pelo nome EXATO da tag (tags[].name) no spec de origem desta API.
# Tag sem entrada aqui mantém a descrição original vinda do backend.
#
# Nome real confirmado no spec de produção (auth.json): "Autenticação".
# O link abaixo usa o formato de âncora confirmado direto no código-fonte
# do Scalar (node_modules/@scalar/workspace-store/dist/navigation/get-navigation-options.js,
# função getNavigationOptions — headings do overview viram
# "{documentId}/description/{slug}") — ver docs/arquitetura.md, decisão 20.

Autenticação:
  description: |
    Endpoints para geração e renovação do token JWT utilizado por **todas as demais APIs** deste portal.

    > [!TIP]
    > **Primeira integração?**
    > * Consulte a [Visão geral da API](#auth/description/1-visão-geral-da-api) para compreender o fluxo completo.
    > * Veja como obter o **Token de Parceiro** e o **Token de Cliente** na seção [Credenciais de acesso](#auth/description/2-credenciais-de-acesso).
    > * Confira a diferença entre as operações **Gerar JWT** e **Atualizar JWT** na seção [Gerenciamento do token JWT](#auth/description/4-gerenciamento-do-token-jwt).